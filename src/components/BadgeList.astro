---
interface Props {
  items: string[];
}
const { items } = Astro.props;
---

<div class="flex flex-wrap gap-2">
  {items.map((item) => (
    <span class="inline-block bg-primary/8 text-primary text-sm px-3 py-1 rounded-full font-medium">
      {item}
    </span>
  ))}
</div>
